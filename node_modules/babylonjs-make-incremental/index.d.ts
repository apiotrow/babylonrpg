export interface OptionProps {
    excludedPaths?: string[];
    excludedMeshes?: {
        [key: string]: RegExp[];
    };
}
export default function makeIncremental(src: string, options?: OptionProps): void;
